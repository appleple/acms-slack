machine:
  node:
    version: 8.9.2

  post:
    - npm install -g npm

  timezone: Asia/Tokyo

  hosts:
    acms.dev: 127.0.0.1

dependencies:
  override:
    - npm install

test:
  override:
    - "true"

deployment:
  production:
    branch:
      - /master
    commands:
      - npm run deploy